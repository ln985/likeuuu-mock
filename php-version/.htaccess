RewriteEngine On

# 如果文件存在，直接访问 (如 admin.php, *.json)
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# 所有 /api/ 开头的请求转发到 index.php
RewriteRule ^api/ index.php [QSA,L]
